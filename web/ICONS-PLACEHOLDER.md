# Icon Placeholders

`icon-192.png` and `icon-512.png` are placeholder assets generated by ImageMagick: a bold "Q" glyph in honey (#e0a458) on an espresso background (#1e1712).

**These must be replaced with the real Keyhole-Q brand marks before any public launch.** They serve only to satisfy the PWA install criteria during development.

When brand assets land, replace both files in `web/static/` and delete this notice.
