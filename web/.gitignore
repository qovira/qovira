node_modules
/.svelte-kit
# adapter-static writes to ../internal/httpx/webdist (gitignored at repo root),
# so no web/build directory is ever produced by this project.
/coverage
.env
.env.*
!.env.example
vite.config.ts.timestamp-*
